## ملخص التطوير في نظام Wood & More

### 1. الصلاحيات والأدوار
- **تخصيص الواجهات حسب الدور**:
  - مهندس موقع (`site_engineer`): حضور وانصراف، تقرير يومي، تقرير مفصل، مشروعات.
  - مدير مهندسين (`site_engineer_manager`): تقارير حضور، تقارير يومية، عهدة المهندسين.
  - محاسب (`accountant`): شاشة العهدة، شاشة الماليات.
  - مسؤول التطبيق (`app_admin`): لوحة تحكم كاملة (مشروعات، مناطق، مباني، وحدات، خامات، مشرفين، مقاولين، مخازن…).
- **التحكم في التعديل والحذف**:
  - تعديل/حذف **تقارير الحضور والانصراف** و**التقارير اليومية** مسموح فقط للمستخدم:
    - البريد: `mouhammedhelal@gmail.com`
    - الدور: `app_admin`
  - بقية المستخدمين لديهم صلاحية عرض فقط.

### 2. الحضور والانصراف
- **شاشة الحضور والانصراف** (`AttendanceScreen`):
  - اختيار مشروع إلزامي من قائمة مشروعات تأتي من التخزين (`getProjects`).
  - طلب صلاحية الموقع ثم تسجيل حضور (`check_in`) أو انصراف (`check_out`) مع:
    - اسم المهندس (من المستخدم الحالي).
    - الوقت والتاريخ والموقع (إحداثيات).
    - ربط السجل بالمشروع (id + name).
- **تقارير الحضور والانصراف** (`AttendanceReportsScreen`):
  - فلترة حسب المدة.
  - عرض في جدول مع اسم المهندس، نوع التسجيل، التاريخ، الوقت، المشروع، الموقع، الملاحظات.
  - زر **إنشاء تقرير** يعرض النتائج في نفس الشاشة مع أزرار:
    - **تصدير PDF** بتنسيق موحد (خط Tajawal، شعار الشركة، ألوان خضراء).
    - **عرض الكل** لإرجاع جميع السجلات في المدى.
  - إضافة حذف سجل الحضور/الانصراف (DELETE على الـ backend) مع تقييد الصلاحيات لمسؤول التطبيق فقط.

### 3. التقارير اليومية (مهندس الموقع)
- **الخطوة 1** (`DailyReportStep1Screen`):
  - حقول:
    - اسم المهندس (معروض فقط، من المستخدم).
    - **اسم المشروع**: قائمة منسدلة بالمشروعات من التخزين، مع خيار:
      - **"أخرى"**: عند اختياره يظهر حقل نص إلزامي لكتابة وصف قصير للمشروع.
      - يتم حفظ اسم المشروع بالشكل: `أخرى (النص الذي كتبه المهندس)` ويُستخدم في جميع التقارير لاحقاً.
    - التاريخ والوقت (تلقائيان للعرض فقط).
    - مكان العمل (نص إلزامي).
    - **تفاصيل تقرير اعمال اليوم** (نص إلزامي).
    - المشرف: قائمة منسدلة محمّلة من `getSupervisors` مع خيار "لايوجد مشرف".
    - المقاولون وعدد العمال:
      - يمكن إضافة أكثر من مقاول.
      - لكل مقاول:
        - اختيار اسم المقاول من قائمة `getContractors` + خيار "لايوجد مقاول".
        - عدد العمال (قائمة 1–12، وليست حقل نصي).
    - خطة عمل الغد (إلزامي).
    - الملاحظات (اختياري).
    - المرفقات:
      - إرفاق مستند (PDF/Word) باستخدام `FilePicker` يحفظ Base64 في `document_path`.
      - إرفاق حتى 3 صور (Base64).
  - ترتيب الحقول تم تنظيمه: بيانات أساسية → المقاولين → خطة الغد → ملاحظات → مرفقات.
- **الخطوة 2** (`DailyReportStep2Screen`):
  - إدارة خامات التقرير:
    - تحميل قائمة الخامات من جدول `materials` (أو من القائمة الافتراضية على الويب).
    - إدخال الكميات والوحدات من قائمة `materialUnits`.
  - ربط الخصم من مخزن المشروع عند الحفظ (backend).
- **هيكل البيانات** (`DailyReportData`):
  - إضافة:
    - `id` للتقرير (من قاعدة البيانات).
    - `contractors` كقائمة من `ContractorWorkers` (مقاول + عدد عمال).
    - تخزين `contractors_json` في جدول `daily_reports` (SQLite و PostgreSQL).
  - الحفاظ على حقول سابقة (المواد، المصروفات، الصور، المستند، الخ…).

### 4. التقارير اليومية – الإدارة والتقارير
- **شاشة التقارير** (`ReportsScreen`):
  - فلترة حسب:
    - مهندس (من قائمة `getSiteEngineers`).
    - من تاريخ / إلى تاريخ.
    - مشروع (اختياري، من `getProjects`).
  - عرض البطاقات:
    - اسم المهندس.
    - اسم المشروع.
    - تاريخ التقرير.
    - ملخص من الحقول الرئيسة.
  - عرض تفاصيل التقرير اليومية مع:
    - المواد والمصروفات، والخصومات من المخزن، والعهدة.
  - **حذف تقرير يومي**:
    - زر حذف على البطاقة يظهر فقط لمسؤول التطبيق (`canEditDeleteDailyReport`).
    - ينفذ DELETE على `/daily-reports/:id` ويحدث التخزين المحلي.
  - **تصدير PDF**:
    - إنشاء تقرير PDF يومي بنفس هوية الحضور (Tajawal، شعار، ألوان خضراء).
    - يشمل:
      - بيانات المهندس والمشروع والتاريخ.
      - تفاصيل العمل.
      - المواد المستخدمة والكميات والوحدات.
      - المصروفات والقيم.
      - ملاحظة أن القيَّم مصدرها التقارير اليومية.

### 5. الماليات والعهدة
- **جدول العهدة والرصيد** (PostgreSQL / SQLite):
  - `engineer_balance`: رصيد المهندس (ما تدين به الشركة للمهندس أو العكس).
  - `engineer_custody`: حركات العهدة والمصروفات:
    - إضافة أعمدة:
      - `movement_type` (custody / balance_add / balance_withdraw / daily_expense).
      - `document_path` (لإرفاق مستند).
- **Backend** (`server.js`):
  - مسارات:
    - `GET /engineer-balance/:userId` و `POST /engineer-balance`.
    - `POST /custody`: إضافة عهدة + تحديث الرصيد (إنقاص ما تدين به الشركة).
    - `POST /balance-movement`: إضافة/سحب رصيد (لشاشة المحاسب) مع تسجيل في `engineer_custody`.
    - `GET /custody?userId=...`: إرجاع حركات العهدة/الرصيد مع `movement_type`.
  - عند حفظ التقرير اليومي:
    - حساب إجمالي مصروفات التقرير.
    - تسجيلها كحركة `daily_expense` في التقارير (ضمن تقارير العهدة).
    - تعديل رصيد المهندس في `engineer_balance`.
- **شاشة ماليات المحاسب** (`AccountantFinanceScreen`):
  - عرض رصيد كل مهندس.
  - إضافة رصيد أو سحب رصيد من حساب مهندس:
    - تعديل الرصيد في `engineer_balance`.
    - تسجيل الحركة في `engineer_custody` مع `movement_type` مناسب.
  - تقرير مالي حسب المدة:
    - يجمع:
      - مصروفات التقارير اليومية.
      - إضافة رصيد.
      - سحب رصيد.
      - العهدة.
- **شاشة عهدة المدير** (`ManagerCustodyScreen`):
  - إدخال عهدة لمهندس الموقع مع:
    - بيان الصرف.
    - المبلغ.
    - إرفاق مستند (Base64).
  - تقرير بالعهدة مع تصدير PDF مرتب حسب المستخدم والتاريخ.
- **شاشة عهدة المحاسب** (`AccountantCustodyScreen`):
  - اختيار مستخدم أو جميع المستخدمين + مدة.
  - تقرير حركات العهدة + الرصيد أول وآخر المدة.
  - دمج:
    - حركات العهدة (`custody`, `balance_add`, `balance_withdraw`).
    - مصروفات التقارير اليومية (`daily_expense`).
  - تصدير PDF منسق.

### 6. تقرير المقاول
- **شاشة تقرير المقاول** (`ContractorReportScreen`):
  - اختيار المقاول من قائمة أسماء تأتي من `getContractors` + خيار "لايوجد مقاول".
  - اختيار مدة (من / إلى).
  - استرجاع التقارير اليومية في المدة وترشيحها باسم المقاول.
  - عرض:
    - اسم المشروع.
    - عدد العمال (من التقرير).
    - التاريخ واسم المهندس.
  - تصدير PDF بعنوان "تقرير المقاول" بنفس تصميم باقي التقارير.

### 7. المشروع / المناطق / المباني / المخازن
- **جداول PostgreSQL و SQLite**:
  - `projects`: قائمة المشاريع (تمت إضافة بذور عديدة).
  - `zones`: مناطق داخل المشروع (تُستخدم كأماكن عمل / فيلات).
  - `buildings`: مبانٍ/وحدات داخل المنطقة (تُستخدم كبرجولات في التقرير المفصل).
  - `units`, `building_materials`, `building_cutlist_images`: لإدارة النماذج والمواد والقطعيات لكل مبنى.
  - `project_stock`, `project_stock_ledger`: مخزن المشروع وأرصدة الخامات وسجل الحركات.
- **توحيد المشاريع**:
  - في التخزين المحلي و API:
    - `getProjects()` يعيد قائمة فريدة حسب الاسم (إزالة التكرار).
  - عند حذف مشروع:
    - حذف مناطق المشروع ومخزونه المرتبط.
- **شاشة مشروعات المهندس** (`EngineerProjectsScreen`):
  - اختيار مشروع → منطقة → مبنى.
  - عرض تشوينات المخزن، النماذج، القطعيات.

### 8. التقرير المفصل (مهندس الموقع)
- **قاعدة البيانات**:
  - `work_phases`: جدول مراحل العمل مع بذور:
    - تركيب حديد.
    - تركيب wpc.
    - معالجة ودهان.
  - `detailed_reports`: رأس التقرير:
    - user_id, user_name, report_datetime, project_id, supervisor_id, created_at.
  - `detailed_report_lines`: سطور التقرير:
    - contractor_id، عدد عمال المقاول (1–10)، عمال ذاتي (0–10).
    - zone_id (فيلا)، building_id (برجولة)، phase_id (مرحلة)، workers_count (عدد العمال في هذا السطر).
  - Migration `003_detailed_reports.sql` لإضافة الجداول والبذور لمشاريع Neon.
  - إضافة بيانات تجريبية لمشروع **Cairo gate_ACC_W**:
    - فيلات D1–D5.
    - داخل كل فيلا برجولة shed01 و shed02.
- **Backend API**:
  - `GET /work-phases`: إرجاع مراحل العمل.
  - `POST /detailed-reports`: إنشاء تقرير مفصل + السطور.
  - `GET /detailed-reports`: استرجاع التقارير المفصلة مع السطور (فلترة بالتاريخ/المهندس/المشروع).
- **Flutter – النماذج والتخزين**:
  - `WorkPhaseModel`, `DetailedReportLineModel`, `DetailedReportModel`.
  - في `ApiStorageService` و `DatabaseService`:
    - `getWorkPhases()`, `addDetailedReport()`, `getDetailedReports()`.
  - في `WebStorageService`:
    - `getWorkPhases()` يعيد قائمة ثابتة من 3 مراحل.
    - `addDetailedReport` غير مدعوم على الويب بدون backend (يرمي خطأ إرشادي).
- **واجهة التقرير المفصل** (`DetailedReportScreen`):
  - اسم المهندس (من المستخدم).
  - مشروع إلزامي (من `getProjects`).
  - مشرف اختياري (من `getSupervisors`).
  - مقاولون وتوزيع العمال:
    - لكل مقاول:
      - اختيار المقاول (من `getContractors`).
      - عدد عمال المقاول (1–10).
      - عمال ذاتي (0–10).
      - سطور توزيع:
        - مكان العمل (فيلا: من `getZones(projectId)`).
        - البرجولة (من `getBuildings(zoneId)`).
        - المرحلة (من `getWorkPhases`).
        - عدد العمال في هذا السطر (1–10).
    - يمكن إضافة أكثر من مقاول وأكثر من سطر توزيع لكل مقاول.
  - حفظ التقرير:
    - إرسال رأس التقرير + السطور إلى `addDetailedReport`.
    - استخدام وقت وتاريخ الإنشاء تلقائياً.

### 9. توحيد القوائم المنسدلة
- **مصدر موحد للبيانات المرجعية**:
  - المشاريع: `getProjects()`.
  - المشرفون: `getSupervisors()`.
  - المقاولون: `getContractors()`.
  - مهندسو المواقع: `getSiteEngineers()`.
  - المستخدمون: `getUsers()`.
  - المناطق والمباني: `getZones(projectId)`, `getBuildings(zoneId)`.
  - مراحل العمل: `getWorkPhases()`.
- **إزالة القوائم الثابتة** لأسماء المقاولين والمشرفين أينما وُجدت تقريباً:
  - التقرير اليومي: صار يعتمد على `getSupervisors` و `getContractors` (مع احتياطي بسيط عند فشل التحميل).
  - تقرير المقاول: صار يعتمد على `getContractors`.
  - التقرير المفصل: يعتمد بالكامل على نفس المصدر.
- **قاعدة داخل `.cursor/rules/reference-data.mdc`**:
  - توثيق أن أي شاشة جديدة يجب أن تستخدم هذه الخدمات الموحدة لتعبئة القوائم المنسدلة.

### 10. تحسينات عامة
- جميع الشاشات الجديدة تستخدم:
  - تصميم موحد (ألوان Wood & More – أخضر أبيض).
  - خط عربي مناسب (Tajawal في تقارير الـ PDF).
  - رسائل خطأ وتحذير واضحة (SnackBar) عند فشل التحميل أو الحفظ.
- **التخزين**:
  - `initStorage()` يختار بين:
    - Backend API (PostgreSQL / Neon).
    - تخزين ويب (`WebStorageService`) مع بيانات افتراضية.
    - SQLite محلي (`DatabaseService`) على الموبايل/السطح المكتب.
- **مزامنة تغييرات قاعدة البيانات**:
  - تحديث `init-db.sql` لكل الجداول الجديدة + البذور.
  - ملفات migrations منفصلة (مثل `001_add_engineer_custody_movement_type.sql`, `002_add_daily_reports_contractors_json.sql`, `003_detailed_reports.sql`) مع تعليمات تشغيلها في Neon.

