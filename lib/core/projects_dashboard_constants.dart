/// Projects Dashboard sheet/notes storage variants.
class ProjectsDashboardVariant {
  ProjectsDashboardVariant._();

  /// Existing icon — WebDAV (Excel saves directly to server).
  static const String webdav = 'webdav';

  /// Temporary +1 icon — download, edit locally, re-upload.
  static const String upload = 'upload';
}
