# Projects Dashboard — تجربة الطريقتين (WebDAV vs تحميل/رفع)

هذا الدليل لاختبار الأيقونتين المؤقتتين على **ويب ويندوز** قبل اختيار الطريقة الفائزة.

| الأيقونة | الطريقة | ملف Excel | ملاحظات |
|----------|---------|-----------|---------|
| **Projects Dashboard** | WebDAV — Excel يحفظ مباشرة على السيرفر | `variant=webdav` | منفصلة |
| **Projects Dashboard +1** | تحميل → Excel → رفع يدوي | `variant=upload` | منفصلة |

**الويب:** [https://wood-more-dtnp.onrender.com/](https://wood-more-dtnp.onrender.com/)  
**الـ API:** من `assets/config.json` (مثلاً `https://wood-more-1qtv.onrender.com/`)

---

## 1. النشر

### أ) نشر الـ API (Backend)

```bash
cd backend
git add .
git commit -m "Projects Dashboard: WebDAV + upload variants for A/B test"
git push
```

انتظر حتى يكتمل نشر خدمة **wood-more-api** على Render.

**اختياري (موصى به للإنتاج):** في Render → Environment للـ API:

```text
PD_PUBLIC_API_BASE_URL=https://wood-more-1qtv.onrender.com
PD_WEBDAV_SECRET=<سلسلة عشوائية طويلة>
```

بدون `PD_PUBLIC_API_BASE_URL` يُبنى رابط WebDAV من عنوان الطلب الحالي.

### ب) نشر الويب (Flutter Web)

```bash
cd d:\Helal\FlutterProjects\wood_and_more_app
flutter build web --release
```

ثم ادفع التغييرات إلى الفرع الذي ينشر **wood-more-web** على Render (أو اتبع سير عمل النشر المعتاد في المشروع).

### ج) ترحيل قاعدة البيانات

عند تشغيل الـ API، `ensureProjectsDashboardTables` يضيف عمود `variant` تلقائياً.

للتشغيل اليدوي في Neon SQL Editor:

```sql
-- محتوى backend/migrations/020_projects_dashboard_variants.sql
```

---

## 2. من يستطيع الاختبار؟

- **المكتب الفني** — رفع أولي + تعديل + ملاحظات
- **مدير العمليات** — فتح/تعديل + ملاحظات
- **المدير الرئيسي** (`mouhammedhelal@gmail.com`) — عرض ملاحظات الطرفين

على **الهاتف** تظهر رسالة: «استخدم الكمبيوتر للوصول لهذه الميزة».

---

## 3. اختبار Projects Dashboard (WebDAV)

### الإعداد

1. سجّل دخول من **Chrome على ويندوز**.
2. افتح أيقونة **Projects Dashboard**.
3. من حساب **المكتب الفني**: ارفع ملف `.xlsx` (أول مرة فقط).

### التعديل

1. اضغط **«فتح في Excel (WebDAV)»**.
2. إذا طلب Windows/Excel تأكيداً أمنياً، وافق على فتح الملف من الإنترنت.
3. عدّل خلية واضحة.
4. احفظ بـ **Ctrl+S** في Excel.
5. ارجع للمتصفح واضغط **«تحديث الحالة»** — يجب أن يتغير وقت آخر تحديث.

### ملاحظات

- أضف ملاحظة من المكتب الفني.
- من مدير العمليات: افتح نفس الأيقونة وتحقق من ظهور ملاحظة الطرف الآخر.
- أضف ملاحظة رد من مدير العمليات.

### ما تسجّله

| # | السؤال | نعم / لا / ملاحظة |
|---|--------|-------------------|
| 1 | فتح Excel بنجاح؟ | |
| 2 | الحفظ بـ Ctrl+S يحدّث السيرفر؟ | |
| 3 | السرعة مقبولة؟ | |
| 4 | ظهرت تحذيرات أمنية مزعجة؟ | |

---

## 4. اختبار Projects Dashboard +1 (تحميل/رفع)

### الإعداد

1. افتح أيقونة **Projects Dashboard +1** (ملف **منفصل** عن الأيقونة الأولى).
2. من **المكتب الفني**: ارفع ملف `.xlsx` (يمكن نفس الملف أو نسخة للمقارنة).

### التعديل

1. اضغط **«تحميل الملف»** — يُحفظ في مجلد التنزيلات.
2. أو اضغط **«فتح مباشرة في Excel»** (`ms-excel:`).
3. عدّل في Excel واحفظ محلياً (**Ctrl+S**).
4. في المتصفح: **«رفع النسخة المحدّثة»** واختر الملف المعدّل.
5. **«تحديث الحالة»** — تحقق من وقت التحديث.

### ملاحظات

- أضف ملاحظة (منفصلة عن أيقونة WebDAV).
- تأكد أن ملاحظات +1 لا تظهر في الأيقونة الأخرى.

### ما تسجّله

| # | السؤال | نعم / لا / ملاحظة |
|---|--------|-------------------|
| 1 | التحميل والفتح المباشر يعملان؟ | |
| 2 | الرفع بعد التعديل سهل؟ | |
| 3 | هل نسيت رفع النسخة بعد الحفظ محلياً؟ | |
| 4 | السرعة مقبولة؟ | |

---

## 5. مقارنة سريعة بعد التجربة

| المعيار | WebDAV | +1 تحميل/رفع |
|---------|--------|---------------|
| سهولة الاستخدام | | |
| موثوقية الحفظ | | |
| السرعة | | |
| ملاءمة الفريق | | |

**القرار:** احتفظ بأيقونة واحدة واحذف الأخرى (أخبر المطور أي `variant` يبقى: `webdav` أو `upload`).

---

## 6. استكشاف الأخطاء

| المشكلة | الحل |
|---------|------|
| Excel لا يفتح (WebDAV) | تأكد من تثبيت Microsoft Excel على ويندوز؛ جرّب فتح الرابط من Chrome وليس من الهاتف |
| 401 / invalid_token | اضغط «فتح في Excel» من جديد (الرمز صالح 24 ساعة) |
| الحفظ لا يظهر بعد Ctrl+S | أغلق Excel ثم «تحديث الحالة»؛ تحقق من سجلات API على Render |
| ms-excel لا يعمل (+1) | استخدم «تحميل الملف» وافتح الملف يدوياً من التنزيلات |
| الأيقونة +1 غير ظاهرة | من **Icons Control** فعّل `projects_dashboard_plus1` للدور، أو امسح ترتيب الأيقونات المحفوظ |
| `no_sheet` | ارفع ملفاً أولاً من المكتب الفني |

### سجلات Render

```text
Dashboard → wood-more-api → Logs
```

ابحث عن أخطاء `projects-dashboard/webdav` أو `PUT /projects-dashboard/sheet`.

---

## 7. بعد اختيار الطريقة الفائزة

1. أخبر فريق التطوير بالأيقونة المحذوفة.
2. يُزال Luckysheet نهائياً من الكود (ما زال موجوداً مؤقتاً وغير مستخدم).
3. تُدمج الأيقونة الفائزة وتُحذف الأيقونة التجريبية من `icon_visibility_service` و`home_icon_builder`.

---

## 8. أوامر محلية للتحقق السريع

```bash
# تحليل Flutter
flutter analyze lib/screens/projects_dashboard_screen.dart lib/screens/projects_dashboard_plus1_screen.dart

# تشغيل API محلياً (يتطلب backend/.env مع DATABASE_URL)
cd backend
node server.js
```

```bash
# بناء ويب للإنتاج
flutter build web --release
```
