-- =============================================================================
-- ترحيل: إضافة عمود نوع الحركة ومسار المستند لجدول engineer_custody
-- =============================================================================
-- متى تشغّل هذا الملف؟
--   عندما قاعدة البيانات على Neon (أو أي PostgreSQL) موجودة مسبقاً، وتم إنشاء
--   جدول engineer_custody بدون الأعمدة movement_type و document_path.
--   (مطلوب حتى تظهر حركات "إضافة رصيد" و"سحب رصيد" في تقرير المحاسب.)
--
-- كيف تنفّذه على Neon؟
--   1. ادخل إلى لوحة Neon: https://console.neon.tech
--   2. اختر المشروع ثم الفرع (Branch) الذي فيه قاعدة البيانات.
--   3. من القائمة الجانبية: SQL Editor (أو من الصفحة الرئيسية للفرع).
--   4. انسخ والصق محتوى هذا الملف في المحرر.
--   5. اضغط Execute (أو Run).
--   6. إذا ظهرت رسالة نجاح أو "Success" بدون أخطاء، انتهى.
--
-- يمكن تشغيل هذا الملف أكثر من مرة دون مشكلة (IF NOT EXISTS).
-- =============================================================================

ALTER TABLE engineer_custody ADD COLUMN IF NOT EXISTS movement_type TEXT DEFAULT 'custody';
ALTER TABLE engineer_custody ADD COLUMN IF NOT EXISTS document_path TEXT;
