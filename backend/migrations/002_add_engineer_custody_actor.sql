-- =============================================================================
-- ترحيل: إضافة بيانات منفّذ الحركة لجدول engineer_custody
-- =============================================================================
-- متى تشغّل هذا الملف؟
--   عندما قاعدة البيانات على Neon (أو أي PostgreSQL) موجودة مسبقاً، وتريد أن
--   يعرض «سجل الحركات» في شاشة العهدة/المصروفات مَن قام بإضافة أو سحب الرصيد.
--   (بدون هذه الأعمدة لن تظهر حركات الأرصدة في السجل إطلاقاً.)
--
-- ملاحظة: الخادم ينفّذ نفس الأعمدة تلقائياً عند الإقلاع، فهذا الملف للتشغيل
--   اليدوي فقط إن أردت تجهيز القاعدة قبل رفع النسخة الجديدة.
--
-- كيف تنفّذه على Neon؟
--   1. ادخل إلى لوحة Neon: https://console.neon.tech
--   2. اختر المشروع ثم الفرع (Branch) الذي فيه قاعدة البيانات.
--   3. من القائمة الجانبية: SQL Editor.
--   4. انسخ والصق محتوى هذا الملف في المحرر ثم اضغط Execute.
--
-- الحركات المسجّلة قبل هذا الترحيل ستبقى بلا منفّذ ولن تظهر في السجل.
-- يمكن تشغيل هذا الملف أكثر من مرة دون مشكلة (IF NOT EXISTS).
-- =============================================================================

ALTER TABLE engineer_custody ADD COLUMN IF NOT EXISTS actor_user_id INTEGER;
ALTER TABLE engineer_custody ADD COLUMN IF NOT EXISTS actor_user_name TEXT;
ALTER TABLE engineer_custody ADD COLUMN IF NOT EXISTS actor_role TEXT;
